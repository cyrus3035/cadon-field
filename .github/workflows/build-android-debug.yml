name: Build CADonField Debug APK (Gradle Cloud)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 24.x
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Patch react-native-webview/index.js ESM -> CJS (Node 24 compatibility)
      run: |
        f="node_modules/react-native-webview/index.js"
        if [ -f "$f" ]; then
          cat > "$f" << 'PATCH_EOF'
        const WebView = require('./lib/WebView').default;
        module.exports = WebView;
        module.exports.WebView = WebView;
        module.exports.default = WebView;
        PATCH_EOF
          echo "--- Patched react-native-webview/index.js to CJS ---"
        else
          echo "--- WARNING: $f not found ---"
        fi

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Install Android SDK packages
      run: sdkmanager "build-tools;34.0.0" "platforms;android-35"

    - name: Run expo prebuild
      run: npx expo prebuild --platform android --no-install

    - name: Verify app_name in strings.xml (software name check)
      run: |
        echo "--- Checking app_name in strings.xml ---"
        grep -n 'app_name' android/app/src/main/res/values/strings.xml

    - name: Fix Kotlin/Compose version compatibility
      run: |
        cat >> android/build.gradle << 'EOF'

        subprojects {
            tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
                kotlinOptions {
                    freeCompilerArgs += ["-P", "plugin:androidx.compose.compiler.plugins.kotlin:suppressKotlinVersionCompatibilityCheck=true"]
                }
            }
        }
        EOF
        echo "--- Added suppressKotlinVersionCompatibilityCheck ---"

    - name: Force JS bundle embedding in debug build
      run: |
        echo "--- Forcing debuggableVariants = [] ---"
        sed -i.bak '/react {/a\    debuggableVariants = []' android/app/build.gradle
        echo "--- Verifying react block in build.gradle ---"
        grep -A5 'react {' android/app/build.gradle

    - name: Build Android APK (NODE_OPTIONS fixes Node 24 ESM compatibility)
      run: |
        cd android
        NODE_OPTIONS="--experimental-vm-modules" ./gradlew assembleDebug -PbundleInDebug=true --no-daemon
        cd ..
        echo "--- APK output listing ---"
        ls -lh android/app/build/outputs/apk/debug/
        echo "--- Check bundle inside APK ---"
        unzip -l android/app/build/outputs/apk/debug/app-debug.apk | grep -i "bundle" || echo "WARNING: no bundle found"

    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: CADonField-debug-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 14